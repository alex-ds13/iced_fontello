[package]
name = "iced_fontello"
version = "0.15.0-dev"
edition = "2024"
description = "Generate type-safe icon fonts for `iced` at compile time"
repository = "https://github.com/hecrj/iced_fontello"
license = "MIT"
categories = ["gui"]
keywords = ["gui", "ui", "graphics", "interface", "widgets"]
rust-version = "1.92"

[dependencies]
rand = "0.8.5"
reqwest = { version = "0.13", features = ["blocking", "json", "multipart"] }
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
svg = "0.18"
svg_path_ops = "0.8.0"
toml = "0.9"
zip = "8"

[workspace]
members = ["example"]

[workspace.dependencies]
iced_fontello = { version = "0.15.0-dev", path = "." }
